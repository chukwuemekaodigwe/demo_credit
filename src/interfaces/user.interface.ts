export default interface User {
    id?: string,
    firstname?: string,
    lastname?: string,
    email?: string,
    phone?: number,
    address?: string,
    password?: string,
    created_at?: Date,
    updated_at?: Date
}